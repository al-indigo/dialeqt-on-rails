<%= form_for(@word_sound) do |f| %>
  <% if @word_sound.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@word_sound.errors.count, "error") %> prohibited this word_sound from being saved:</h2>

      <ul>
      <% @word_sound.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word_id %><br>
    <%= f.collection_select(:word_id, Word.all, :id, :transcription) %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field"><%= f.file_field :sound %></div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
