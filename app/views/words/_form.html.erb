<%= form_for(@word, :html => {:multipart => true}) do |f| %>
  <% if @word.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@word.errors.count, "error") %> prohibited this word from being saved:</h2>

      <ul>
      <% @word.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :word %><br>
    <%= f.text_field :word %>
  </div>
  <div class="field">
    <%= f.label :transcription %><br>
    <%= f.text_field :transcription %>
  </div>
  <div class="field">
    <%= f.label :translation %><br>
    <%= f.text_field :translation %>
  </div>
  <div class="field">
    <%= f.label :tag %><br>
    <%= f.text_field :tag %>
  </div>
  <div class="field">
      <%= f.label :dictionary_id %><br>
      <%= f.collection_select(:dictionary_id, Dictionary.all, :id, :name) %>
  </div>
<!--  <%= f.file_field :attachable %> -->
  <div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
